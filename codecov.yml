coverage:
  round: down
  range: "60...100"
  status:
    project:
      default:
        target: 60%
        threshold: 5
        base: auto
        if_ci_failed: error
parsers:
  cobertura:
    partials_as_hits: false
    handle_missing_conditions: false
